# Env for Filesystem connector. Pin the image and set scan/quarantine paths. Copy to .env and point compose via --env-file.

FILESYSTEM_IMAGE=dsxconnect/filesystem-connector:0.5.50  # replace :tag value to use a different version

# DSXCONNECTOR_ASSET is the host path to scan; compose binds it to /app/scan_folder.
DSXCONNECTOR_ASSET=</path/to/local/folder> # replace with path to a accessible folder
# DSXCONNECTOR_ITEM_ACTION_MOVE_METAINFO is the host path for quarantine/move actions; compose binds it to /app/quarantine.
DSXCONNECTOR_ITEM_ACTION_MOVE_METAINFO=</path/to/local/folder/dsxconnect-quarantine> # replace with path to an accessible folder where files will be moved on quarantine

# Optional overrides
#DSXCONNECTOR_FILTER=
#DSXCONNECTOR_ITEM_ACTION=nothing
