apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  # Name must match: <release>-salesforce-connector-tls
  # Example for Helm release name "sf-dev":
  name: sf-dev-salesforce-connector-tls
  namespace: default
data:
  # Base64-encoded PEM contents
  tls.crt: "REPLACE_WITH_BASE64_CERT"
  tls.key: "REPLACE_WITH_BASE64_KEY"

# Tip: You can also create the Secret via kubectl:
#   kubectl create secret tls sf-dev-salesforce-connector-tls \
#     --cert=tls.crt --key=tls.key -n default
