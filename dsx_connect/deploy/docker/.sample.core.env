# Env for DSX-Connect core. Pin image tags and supply optional DSXA/auth settings. Copy to xxxx.core.env and set values per environment (dev/stage/prod).

# Core env (sample)
DSXCONNECT_IMAGE=dsxconnect/dsx-connect:0.3.66              # used by docker-compose-dsx-connect-all-services.yaml
DSXA_IMAGE=dsxconnect/dpa-rocky9:4.1.1.2020                 # used by docker-compose-dsxa.yaml
#DSXCONNECT_ENROLLMENT_TOKEN=your-token
#APPLIANCE_URL=https://<di>.customers.deepinstinctweb.com   # used by docker-compose-dsxa.yaml
#TOKEN=<DSXA token>                                         # used by docker-compose-dsxa.yaml
#SCANNER_ID=<scanner id>                                    # used by docker-compose-dsxa.yaml
#FLAVOR=rest,config                                         # optional, used by docker-compose-dsxa.yaml
#NO_SSL=true                                                # optional, used by docker-compose-dsxa.yaml
#HOST_PORT=15000                                            # optional host port override, used by docker-compose-dsxa.yaml

# Syslog (enabled by default in compose; points to bundled rsyslog)
DSXCONNECT_SYSLOG__SYSLOG_SERVER_URL=syslog                 # used by docker-compose-dsx-connect-all-services.yaml
DSXCONNECT_SYSLOG__SYSLOG_SERVER_PORT=514                   # used by docker-compose-dsx-connect-all-services.yaml
DSXCONNECT_SYSLOG__TRANSPORT=tcp                            # used by docker-compose-dsx-connect-all-services.yaml
